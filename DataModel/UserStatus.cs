namespace SACEology
{
    public enum UserStatus
    {
        /// <summary>
        /// The ID representing that the current user is a student.
        /// </summary>
        Student = 0,

        /// <summary>
        /// The ID representing that the current user is a teacher.
        /// </summary>
        Teacher = 1
    }
}
